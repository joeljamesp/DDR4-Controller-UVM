# results/

Simulation output lands here: waveform dumps (`*.vcd`/`*.wdb`), run logs, and
any coverage reports a run produces. These are simulator output, not source,
so they're gitignored except for this placeholder — run `make xsim TEST=...`
(or `questa`/`vcs`) from `sim/` to populate it.
